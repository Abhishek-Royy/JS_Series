In javascript Array is a special type of- Object.

** There are multiple areay methods in js:-
            push(): if we wand to add somthing at end position.
            pop(): delete from end and return
            toString(): convert array to String